# S3 Events handled by Lambda

This example demonstrate the s3 events that trigger Lambda function.

### Create Bundle

For windows with power shell

```sh
Compress-Archive -Path .\function\ -DestinationPath .\function.zip -Force
```

### Running Terraform

```sh
terraform init
terraform apply
```
